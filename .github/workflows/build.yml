name: C/C++ CI

on:
  workflow_dispatch:
#  push:
#    branches: [ "main" ]
#  pull_request:
#    branches: [ "main" ]
  

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - uses: 'joncloud/download-install-action@main'
      with:
#        checksum-url: 'https://releases.hashicorp.com/terraform/1.5.3/terraform_1.5.3_SHA256SUMS'
#        checksum-entry: 'terraform_1.5.3_linux_amd64.zip'
        archive-url: 'https://github.com/SiliconLabs/gecko_sdk/releases/download/v4.3.1/gecko-sdk.zip'
        target-path: /Users/ltarantino/SimplicityStudio/SDKs/gecko_sdk_3/
#        archive-entries: 'terraform'    
#    - name: configure
#      run: ./configure
    - name: make
      run: make -f rcp-uart-802154_2.Makefile release
#    - name: make check
#      run: make check
#    - name: make distcheck
#      run: make distcheck
    - name: create gbl
      run: commander gbl create  "./build/release/rcp-uart-802154_2.gbl" --app "./build/release/rcp-uart-802154_2.s37" 
